#include <iostream>

int main (int argc, char **argv)
{
	/*Drawing *drawing = new DrawingV1();

	Shape *circle = new Circle(drawing, 200, 300, 30);
	Shape *hexagon = new Hexagon(drawing, 400, 20, 40, 40);
	Shape *rect = new Rectangle(drawing, 100, 100, 200, 100, 45.f);

	circle->draw();
	hexagon->draw();
	rect->draw();

	delete drawing;
	delete circle;
	delete hexagon;
	delete rect;*/

	return 0;
}